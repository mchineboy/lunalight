MEMORY {
    HEADER: start = $0000, size = $0002, file = %O;
    MUSIC:  start = $4200, size = $1000, file = %O;
}

SEGMENTS {
    LOADADDR: load = HEADER, type = ro;
    CODE:     load = MUSIC,  type = rw;
}
